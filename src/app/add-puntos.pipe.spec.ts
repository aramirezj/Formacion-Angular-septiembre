import { AddPuntosPipe } from './add-puntos.pipe';

describe('AddPuntosPipe', () => {
  it('create an instance', () => {
    const pipe = new AddPuntosPipe();
    expect(pipe).toBeTruthy();
  });
});
