import { DecoraEtiquetasDirective } from './decora-etiquetas.directive';

describe('DecoraEtiquetasDirective', () => {
  it('should create an instance', () => {
    const directive = new DecoraEtiquetasDirective();
    expect(directive).toBeTruthy();
  });
});
