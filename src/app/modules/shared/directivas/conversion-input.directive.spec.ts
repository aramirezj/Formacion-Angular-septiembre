import { ConversionInputDirective } from './conversion-input.directive';

describe('ConversionInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ConversionInputDirective();
    expect(directive).toBeTruthy();
  });
});
